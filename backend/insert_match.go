package backend

import (
	"context"

	"github.com/benspotatoes/extrawatch/models"
)

func (b *backendImpl) InsertMatch(ctx context.Context, params *models.InsertParams) error {
	return errNotImplemented
}
